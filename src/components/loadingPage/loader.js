import React from 'react';

import './loader.css'

const Loader = () => {
    return (
        <div className='loader-container'>
            <h2>loading...</h2>
        </div>
    );
};

export default Loader;